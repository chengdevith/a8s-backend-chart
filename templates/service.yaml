apiVersion: v1
kind: Service
metadata:
  name: {{ include "a8s-backend.fullname" . }}
  labels:
    {{- include "a8s-backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "a8s-backend.selectorLabels" . | nindent 4 }}
