apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "a8s-backend.fullname" . }}-test-connection"
  labels:
    {{- include "a8s-backend.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "a8s-backend.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
